<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Alarmes exatos: obrigatorio para disparar na hora certa -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Reagendar tudo depois de reiniciar o aparelho -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Notificacao de aviso quando chega a hora -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Pedir isencao de otimizacao de bateria (Xiaomi, Samsung, etc.) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Android 11+: sem isto o app nao "enxerga" o WhatsApp instalado -->
    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".ZapApp"
        android:allowBackup="false"
        android:icon="@android:drawable/ic_dialog_email"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZapAgendado">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity invisivel que abre o WhatsApp na hora marcada -->
        <activity
            android:name=".envio.EnvioActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:showOnLockScreen="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Invisivel" />

        <receiver
            android:name=".agenda.AlarmeReceiver"
            android:exported="false" />

        <receiver
            android:name=".agenda.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- OPCIONAL: toque no botao enviar automaticamente. Leia o README antes de usar. -->
        <service
            android:name=".envio.AutoEnvioService"
            android:exported="false"
            android:label="@string/auto_envio_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/auto_envio_config" />
        </service>

    </application>
</manifest>
